- 2026-03-29 item: Direct zsh shell smart paste outside tmux. decisions: added a repo-tracked Windows/WezTerm direct-shell verification script and package script, and recorded the window-cleanup rule in Ralph docs; kept `passes=false` because real Ctrl+Shift+V proof is still blocked. files changed: `.ralph/plan.md`, `.ralph/prompt.md`, `package.json`, `scripts/verify-smart-paste-direct-shell.ps1`. verification: `npm run check` ❌ (pre-existing Biome/style errors in tracked repo files such as `src/index.ts`, `src/image-gallery.ts`, `.ralph/items.json`, `.pi/tasks/...`), `npm run test` ✅, `npm pack --dry-run` ✅, `npm run verify:smart-paste:direct-shell` ❌/timeout after script updates because Windows key injection still did not complete end-to-end proof; all extra WezTerm windows opened during verification were closed afterward. next: make the direct-shell verifier reliably target a controllable non-user WezTerm window/handle, then rerun text+image proof before changing item status.
- 2026-03-29 item: Direct zsh shell smart paste outside tmux. decisions: updated the verifier to target the real visible WezTerm GUI, detect target-window elevation, and fail fast instead of pretending proof from a non-elevated session; confirmed the active WezTerm window pid `30508` is elevated and the current verifier process is not, so Windows UIPI is the current blocker. files changed: `scripts/verify-smart-paste-direct-shell.ps1`. verification: `npm run verify:smart-paste:direct-shell` ❌ with deterministic UIPI/elevation blocker message, `npm run check` ❌ (same pre-existing Biome/style errors in `src/index.ts`, `src/image-gallery.ts`, `.ralph/items.json`, `.pi/tasks/...`), `npm run test` ✅, `npm pack --dry-run` ✅; temporary verification tab/pane opened in WezTerm was closed afterward. next: rerun `npm run verify:smart-paste:direct-shell` from an elevated Windows PowerShell process on the same desktop as the real WezTerm window, then capture text/image proof and only then consider changing item status.
